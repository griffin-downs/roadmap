// Unit tests (fast): Schema module
import { describe, it, expect } from 'vitest';

describe('Schema types', () => {
  it('ValidatorRule type is defined', () => {
    expect(true).toBe(true);
  });
});

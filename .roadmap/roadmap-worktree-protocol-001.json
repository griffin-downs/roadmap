{
  "id": "roadmap-worktree-protocol-001",
  "desc": "Implement worktree-based DAG editing protocol with enforced consolidation. Single unified discipline for all users (human + LLM).",
  "init": "protocol-design",
  "term": "protocol-complete",
  "nodes": {
    "protocol-design": {
      "id": "protocol-design",
      "desc": "Formalize worktree protocol: feature branches, head.json consolidation, merge orchestration",
      "produces": [
        "docs/WORKTREE-PROTOCOL.md"
      ],
      "consumes": [],
      "deps": [],
      "validate": [
        {
          "type": "artifact-exists",
          "path": "docs/WORKTREE-PROTOCOL.md"
        }
      ],
      "idempotent": true,
      "mode": "plan"
    },
    "precommit-hook-enforcement": {
      "id": "precommit-hook-enforcement",
      "desc": "Update pre-commit hook to enforce worktree protocol: reject head.json edits on main, allow on feat/* branches",
      "produces": [
        "scripts/hooks/pre-commit"
      ],
      "consumes": [],
      "deps": [
        "protocol-design"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "grep -q 'feat/' scripts/hooks/pre-commit && grep -q 'main' scripts/hooks/pre-commit"
        }
      ],
      "idempotent": true
    },
    "worktree-spawn-command": {
      "id": "worktree-spawn-command",
      "desc": "Add CLI command: roadmap spawn --task <id> creates worktree + feature branch, ready for agent/user",
      "produces": [
        "bin/roadmap.ts"
      ],
      "consumes": [],
      "deps": [
        "protocol-design"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "npx tsc --noEmit"
        }
      ],
      "idempotent": true
    },
    "merge-orchestrator-command": {
      "id": "merge-orchestrator-command",
      "desc": "Add CLI command: roadmap merge-batch --from <branches> validates and merges feature branches to main",
      "produces": [
        "bin/roadmap.ts"
      ],
      "consumes": [],
      "deps": [
        "precommit-hook-enforcement"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "npx tsc --noEmit"
        }
      ],
      "idempotent": true
    },
    "consolidation-at-merge": {
      "id": "consolidation-at-merge",
      "desc": "Integrate auto-consolidation into merge orchestrator: validate merged DAG, run consolidation, write unified head.json",
      "produces": [
        "src/lib/roadmap/merge-orchestrator.ts"
      ],
      "consumes": [
        "src/lib/roadmap/dag-consolidator.ts"
      ],
      "deps": [
        "merge-orchestrator-command"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "npx tsc --noEmit"
        }
      ],
      "idempotent": true
    },
    "worktree-cleanup": {
      "id": "worktree-cleanup",
      "desc": "Add cleanup: roadmap cleanup-worktrees removes stale worktrees, verifies branches merged",
      "produces": [
        "bin/roadmap.ts"
      ],
      "consumes": [],
      "deps": [
        "consolidation-at-merge"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "npx tsc --noEmit"
        }
      ],
      "idempotent": true
    },
    "onboarding-documentation": {
      "id": "onboarding-documentation",
      "desc": "Update CLAUDE.md with worktree workflow: spawn, edit, commit, merge orchestration, cleanup",
      "produces": [
        ".claude/CLAUDE.md"
      ],
      "consumes": [
        "docs/WORKTREE-PROTOCOL.md"
      ],
      "deps": [
        "worktree-cleanup"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "grep -q 'git worktree add' .claude/CLAUDE.md"
        }
      ],
      "idempotent": true
    },
    "protocol-tests": {
      "id": "protocol-tests",
      "desc": "Unit + integration tests: worktree spawning, feature branch enforcement, consolidation on merge, cleanup",
      "produces": [
        "tests/protocol/worktree-protocol.test.ts"
      ],
      "consumes": [
        "scripts/hooks/pre-commit",
        "src/lib/roadmap/merge-orchestrator.ts"
      ],
      "deps": [
        "onboarding-documentation"
      ],
      "validate": [
        {
          "type": "shell",
          "command": "npx tsc --noEmit && npx vitest run tests/protocol/worktree-protocol.test.ts 2>&1 | grep -q 'passed'"
        }
      ],
      "idempotent": true
    },
    "protocol-complete": {
      "id": "protocol-complete",
      "desc": "Worktree protocol fully enforced: single discipline for all users, transparent consolidation, merge orchestration",
      "produces": [
        ".protocol/system-ready.json"
      ],
      "consumes": [],
      "deps": [
        "protocol-tests"
      ],
      "validate": [
        {
          "type": "artifact-exists",
          "path": ".protocol/system-ready.json"
        }
      ],
      "idempotent": false
    }
  },
  "baseSha": "17ae5d8ff6a949f82ea82f79e20c69db0c44deef"
}

{
  "agentId": "test-enforcement-agent",
  "nodeId": "test-enforcement",
  "dagId": "roadmap-audit-enforcement-001",
  "dagPath": ".roadmap/head.candidate.json",
  "position": {
    "nodeId": "test-enforcement",
    "batch": 4,
    "level": 4
  },
  "mode": "execute",
  "description": "Write tests validating all enforcement gates",
  "produces": ["tests/enforce-audit.test.ts"],
  "consumes": ["src/lib/protocol/batch-invariants.ts", "src/lib/evidence/completion-enforcer.ts"],
  "dependencies": ["enforce-batch-invariants", "enforce-completion-sync"],
  "validate": [
    {
      "type": "artifact-exists",
      "path": "tests/enforce-audit.test.ts"
    },
    {
      "type": "shell",
      "command": "npm run test -- tests/enforce-audit.test.ts 2>&1 | tail -10",
      "expectExitCode": 0
    }
  ],
  "instructions": {
    "intent": "Write comprehensive test suite for batch-invariants and completion-enforcer modules. Test suite must cover: happy path (valid batch, complete sync), error cases (missing deps, missing artifacts, async violations), and edge cases (concurrent claims, partial writes, recovery after failure).",
    "acceptance_criteria": [
      "tests/enforce-audit.test.ts exists",
      "Min 18 test cases: 6 batch-invariants + 8 completion-enforcer + 4 integration",
      "All tests pass: npm run test -- tests/enforce-audit.test.ts",
      "Coverage > 85% for batch-invariants.ts and completion-enforcer.ts",
      "Test names follow pattern: describe(moduleName), it('should enforce invariant: ...')"
    ],
    "context": {
      "repoRoot": "/home/griffin/src/roadmap",
      "relatedFiles": [
        "src/lib/protocol/batch-invariants.ts",
        "src/lib/evidence/completion-enforcer.ts",
        "tests/protocol/",
        "tests/validation/"
      ],
      "testScenarios": {
        "batchInvariants": [
          "Valid batch passes all checks",
          "Node with missing dependency fails",
          "Duplicate claims detected",
          "Invalid topological order detected",
          "Artifacts missing from previous batch"
        ],
        "completionEnforcer": [
          "All produces exist → completes successfully",
          "Missing produce file → rejection",
          "Uncommitted produces → rejection",
          "Timestamp mismatch → warning",
          "Recovery from partial completion"
        ],
        "integration": [
          "Full batch→complete→advance cycle",
          "Concurrent worker conflicts",
          "Rollback and retry"
        ]
      }
    }
  }
}
